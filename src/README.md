# Dependabom Scripts

Standalone Python CLIs for finding Maven BOM usage and detecting BOM-managed dependency overrides.

## Install Python Requirements

From the repository root:

```bash
python3 -m pip install -r requirements.txt
```
